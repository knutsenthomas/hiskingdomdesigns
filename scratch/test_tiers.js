import { tiers, rewards, programs } from '@wix/loyalty';

console.log('Tiers methods:', Object.keys(tiers));
console.log('Rewards methods:', Object.keys(rewards));
console.log('Programs methods:', Object.keys(programs));
